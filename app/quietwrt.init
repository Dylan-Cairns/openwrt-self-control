#!/bin/sh /etc/rc.common

START=99

start() {
  [ -x /usr/bin/quietwrtctl ] || return 0
  (
    sleep 15
    if /usr/bin/quietwrtctl boot-check >/tmp/quietwrt-boot-check.log 2>&1; then
      /usr/bin/quietwrtctl sync >/tmp/quietwrt-boot-sync.log 2>&1
    fi
  ) &
}
